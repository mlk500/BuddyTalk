# ============================================
# Deployment Mode Configuration
# ============================================

# Set to 'false' for Vercel/production deployment (audio-only, no backend needed)
# Set to 'true' for local development (enables lip-sync video generation)
# Default: true (local mode with full features)
VITE_ENABLE_LIPSYNC=true

# ============================================
# LLM API Configuration
# ============================================

# OpenRouter API Key
# - LOCAL MODE: Add key here (will be sent from browser)
# - DEPLOYED MODE: Leave empty, add to Vercel environment variables as OPENROUTER_API_KEY
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Upgrade to paid model for better quality
# Default (omit or false): Uses FREE models only (google/gemini-2.0-flash-exp:free + fallbacks)
# Set to true: Uses paid model ($0.10 input, $0.40 output) with free fallbacks
# VITE_OPENROUTER_USE_PAID=false

# ============================================
# Voice & Video APIs
# ============================================

# Fish Audio API Configuration
# - LOCAL MODE: Add key here (will be sent from browser)
# - DEPLOYED MODE: Leave empty, add to Vercel environment variables as FISH_AUDIO_API_KEY
VITE_FISH_AUDIO_API_KEY=your_fish_audio_api_key_here

# Wav2Lip Backend API URL (LOCAL MODE ONLY)
# Only used when VITE_ENABLE_LIPSYNC=true
VITE_WAV2LIP_API_URL=http://localhost:8000

# ============================================
# Database Configuration (REQUIRED for both modes)
# ============================================

# Supabase Configuration - Get yours at https://supabase.com
# Required for user profiles, chat history, and memory features
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================
# Optional APIs (for advanced features)
# ============================================

# Claude API Key - Optional, only needed for memory extraction and level assessment
# Get yours at https://console.anthropic.com/
# VITE_CLAUDE_API_KEY=your_claude_api_key_here
